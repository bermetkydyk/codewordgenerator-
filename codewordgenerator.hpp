#ifndef CODEWORD_GENERATOR_
#define CODEWORD_GENERATOR_
#include <iostream>
#include <queue>
#include <regex>
#include <string>
#include <stack>

using namespace std;

// this impelementation will only have two public functions 
class CodewordGenerator{
public:
    string generateShortestWord(string pattern);
    string generateLengthWord(string pattern, int length);


private:


};

#endif 
